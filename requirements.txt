# FL Toolkit — consolidated Python dependencies for all live tools.
# Add to this file as each tool is migrated; keep versions compatible across tools.

streamlit>=1.40

# PDF Tools (pdfbot — 8 tools):
#   System packages: ghostscript (compress/watermark), poppler-utils (PDF→image)
pypdf>=4.0
reportlab>=4.0
pillow>=10.0
numpy>=1.26
pikepdf>=8.0
openpyxl>=3.1
pdf2image>=1.17

# Shipping Labels (uniqlo-lionParcel) + Bank Statements + Payroll → BCA:
pandas>=2.0
fpdf2>=2.7

# Rename BUPOT (renameBUPOT) + PDF Tools "PDF to Excel"
# + Bank Statements (BCA personal CSV, credit-card PDF extractors):
pdfplumber>=0.11

# Deferred tools (add when migrated):
#   Phonics (tts-phonics):               google-cloud-texttospeech
#   Tax Tools (tax_tools):               openpyxl, pdfplumber
